# z_minimal 主题架构与迭代指引（AGENTS.md）

本文档全面记录 `z_minimal` WordPress 极简个人博客主题的设计哲学、核心架构、文件职责、技术规范以及后续迭代需遵循的约束原则。

---

## 1. 主题定位与核心设计哲学

- **极致极简与内容优先**：去除了所有冗余的侧边栏、小工具和繁琐特效，注重留白与呼吸感，专注文字与代码阅读。
- **中国大陆极速加载（零外部依赖）**：全站 100% 不依赖 Google Fonts、cdnjs、FontAwesome 或任何境外 CDN，全站资源均为纯本地渲染，毫秒级秒开。
- **现代原生系统字体栈**：采用 macOS/iOS/Windows/Android 原生 UI 字体，配合等宽数字（Tabular Numbers）确保日期排版像素级严谨。
- **科技简约极淡蓝调 + 自动跟随系统三态深浅色**：原生 CSS 变量驱动，浅色采用纯净科技微白底（`#fafbfc`）+ 冰雾浅蓝（`#0ea5e9` / `#f0f7ff`），深色采用深邃极夜黑蓝（`#080c14`）+ 冰晶透光蓝（`#7dd3fc`），配备单一 Icon 循环切换（💻跟随系统 -> ☀️浅色模式 -> 🌙深色模式），`<head>` 内置 Anti-FOUC 防闪烁逻辑。
- **全站严格 SEO 语义化**：遵循黄金法则——每个页面仅有唯一 `<h1>` 标签，配合 HTML5 语义结构与 Schema.org 微数据。

---

## 2. 核心文件与职责划分

```
wp-content/themes/z_minimal/
├── AGENTS.md           # 本文档（主题架构、规范与迭代指引）
├── style.css           # 主题信息声明、CSS 变量系统、排版、组件、深浅色、响应式与打印样式
├── functions.php       # 功能核心：菜单/特性注册、全局禁评论、Customizer配置、SEO Meta、外链处理与智能路由
├── header.php          # 语义化头部：Anti-FOUC 脚本、单 H1 判断、主导航、单一 Icon 主题切换器
├── footer.php          # 语义化页脚：可视化底部链接菜单、工信部/公安备案号直链、动态版权
├── index.php           # 首页模板：页面路由守卫、多行简介、按年不分页归档（YYYY年 / YYYY/MM/DD 标题）
├── single.php          # 文章页模板：Schema 微数据、正文内容、上一篇/下一篇翻页导航
├── page.php            # 通用独立页面模板（关于页等）
├── 404.php             # 简易优雅的 404 错误页
└── screenshot.png      # 后台主题缩略图（1200x900 像素）
```

---

## 3. 核心功能实现细节

### 3.1 导航菜单、分类与关于页面
- **主菜单 (`primary`)**：顶部右侧展示，后台【外观】->【菜单】中自由拖拽（支持自定义链接、页面、分类目录等）。
- **分类目录菜单联动**：点击导航菜单中的分类目录项，自动切换至该分类归档页面：
  - 页面顶部展示语义化分类标题（`<h1>`）与描述；
  - 文章列表自动根据分类 ID 精确筛选，依然保持按年聚类（`YYYY年` -> `YYYY/MM/DD 标题`）且不分页；
  - 首页多行简介仅在主页渲染，分类页面纯净呈现该分类下的文章。
- **关于页面与分类路由容错**：即使未配置服务器伪静态重写（默认朴素固定链接），直接访问 `/about` 或 `/category/slug` 也会由 `z_minimal_smart_slug_fallback()` 智能捕获并加载对应模板，绝不回退至首页。
- **独立归档已被废除**：首页即为按年聚合的完整归档，菜单中无多余“归档”链接。

### 3.2 首页按年归档列表
- 通过 `WP_Query` 设置 `posts_per_page => -1` 全量读取，不进行分页截断。
- 逻辑按发布年份自动聚类，展示格式精确遵循：
  ```text
  2026年
  2026/08/23 博客标题4
  2026/03/28 博客标题3

  2025年
  2025/09/23 博客标题2
  2025/01/18 博客标题1
  ```
- 日期增加 `font-feature-settings: "tnum" 1;` 确保数字绝对等宽对齐。

### 3.3 外观模式与单一 Icon 切换器
- 模式顺序：`system`（跟随系统） -> `light`（浅色模式） -> `dark`（深色模式） -> `system`。
- 图标映射：
  - `system`：显示器矢量图标
  - `light`：太阳矢量图标
  - `dark`：月亮矢量图标
- 防闪烁（Anti-FOUC）：在 `header.php` 的 `<head>` 最上方执行极速检测，在 DOM 绘制前完成 `data-theme` 属性注入，解决页面切换白屏/黑屏闪烁。

### 3.4 文章正文排版与已访问链接区分（Typography & Visited Links）
- **超链接强化与已访问状态区分（`:visited`）**：
  - **正文内链未访问**：主题冰川浅蓝（`--accent-color: #0ea5e9`），搭配 `1.5px` 厚度、`3.5px` 文本偏移下划线；
  - **正文内链已访问**：呈现科技紫罗兰色（`--accent-visited: #6366f1` / 暗色模式 `#c084fc`），使读者清晰识别已读链接；
  - **首页归档文章标题已访问**：点击阅读过的文章标题转为低对比度的已读状态色（`--post-visited: #64748b`），未读文章更突出；
  - **图片链接**：自动排除下划线与背景色。
- **站外链接自动新窗口打开**：服务端 `the_content` 过滤器自动检测站外链接，追加 `target="_blank"` 与 `rel="noopener noreferrer"`；客户端同步进行轻量兜底。
- **图文间距与说明文字**：消除段落与 `<figure>` 的多重外边距堆叠；说明文字（`figcaption`、`.wp-element-caption`）绝对居中，字号弱化为 `0.85rem`，形成清晰层级。
- **数据表格与多媒体**：自适应移动端横向滚动，边框跟随主题变量；嵌入视频自适应容器。
- **高性能原生图片懒加载（Native Lazy Loading & Async Decoding）**：服务端在 `the_content` 输出时自动补全 `loading="lazy"` 与 `decoding="async"`，配合 CSS 微占位底色与平滑渐显过渡，零外部 JS 依赖，显著降低首屏资源消耗并避免解码卡顿。

### 3.5 全局彻底关闭评论
- 过滤器屏蔽 `comments_open`、`pings_open`、`comments_array`。
- 移除文章与页面的评论功能支持，后台菜单及管理工具条中完全剔除评论入口。

### 3.6 自定义代码与统计分析
- 在【外观】->【自定义】->【博客个性化设置】中提供：
  - `z_minimal_header_code`：输出在 `</head>` 之前（用于百度统计、Google Analytics、所有权验证 Meta 等）。
  - `z_minimal_footer_code`：输出在 `</body>` 之前（用于分析 JS 脚本）。

---

## 4. 后台配置速查表

| 功能配置项 | 后台管理入口 | 说明 |
| :--- | :--- | :--- |
| **主导航菜单** | 【外观】->【菜单】 | 勾选“顶部主菜单”，支持拖拽与多级链接 |
| **底部链接菜单** | 【外观】->【菜单】 | 勾选“底部链接菜单”，展示在备案号上方 |
| **首页多行简介** | 【外观】->【自定义】->【博客个性化设置】 | 支持多行回车换行，前台自动以 `<br>` 渲染 |
| **工信部备案号** | 【外观】->【自定义】->【博客个性化设置】 | 填入后前台自动直链工信部官网 `beian.miit.gov.cn` |
| **公安备案号** | 【外观】->【自定义】->【博客个性化设置】 | 可选填入公网安备号 |
| **自定义版权** | 【外观】->【自定义】->【博客个性化设置】 | 留空则默认动态输出 `© 当前年份 站点名称` |
| **页眉/页脚统计代码** | 【外观】->【自定义】->【博客个性化设置】 | 支持多段统计或验证代码同时粘贴 |

---

## 5. 后续迭代开发的红线与规范约束

在未来对 `z_minimal` 进行功能升级或修改时，务必遵守以下规范：

1. **绝对禁止引入外部阻塞资源**：
   - 严禁引入 Google Fonts、外部 CSS 框架（如 Bootstrap / TailwindCDN）或任何外链 Icon 库；
   - 新增图标一律采用原生内嵌 SVG。
2. **严格单 H1 原则**：
   - 首页 `<h1>` 归属于站点名称；
   - 文章页与单页面 `<h1>` 必须独占文章/页面标题，头部站点名自动降级为普通标签；
   - 严禁在页面正文中滥用 `<h1>`。
3. **保持变量色彩继承与深浅色模式一致性**：
   - 新增组件样式必须使用 `var(--bg-color)`、`var(--text-primary)`、`var(--accent-color)` 等主题变量，严禁在组件中写死 `#ffffff` 或 `#000000`。
4. **性能与纯净度**：
   - 新增功能优先采用 WordPress 原生 Hook/Filter 实现，避免增加复杂数据库表或冗余插件依赖。
